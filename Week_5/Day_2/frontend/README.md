# Frontend (Next.js + Tailwind + RTK Query)
- Attractive, responsive UI
- WhatsApp-like threaded comments view (single-level replies)
- JWT persisted in localStorage
- Socket.IO client for realtime notifications

## Run
cp .env.example .env
npm install
npm run dev
