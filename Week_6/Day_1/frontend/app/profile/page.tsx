"use client";

export default function ProfilePage() {
  // The layout handles showing Dashboard / Orders / Settings via sidebar.
  // This page intentionally returns nothing because layout renders tab UI for the root /profile route.
  return null;
}
